import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'portafolio-alejandro-villa',
  webDir: 'www'
};

export default config;
